{
  "name": "@repo/api-schemas",
  "version": "0.3.0",
  "description": "Shared Zod schemas + error envelope for the platform API",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@outerlayer/session-schema": "workspace:*",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "typescript": "^5.5.3",
    "vitest": "^4.1.8"
  }
}
