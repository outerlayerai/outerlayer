{
  "name": "@repo/api-key-service",
  "private": true,
  "version": "0.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "description": "Shared Unkey + Supabase primitives for API-key creation, update, and metadata. Used by both the cloud gateway (Cloudflare Worker, gateway-role client) and the tenant dashboard (Next.js, user-JWT or admin client). Consumers supply their own Supabase and Unkey instances.",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:mutate": "stryker run"
  },
  "devDependencies": {
    "@repo/typescript-config": "*",
    "@stryker-mutator/core": "9.6.1",
    "@stryker-mutator/vitest-runner": "9.6.1",
    "typescript": "^5.5.3",
    "vitest": "^4.1.8"
  }
}
