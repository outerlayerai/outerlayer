{
  "name": "@repo/git-providers",
  "version": "0.0.0",
  "description": "Shared git-provider logic (token crypto, OAuth refresh) used by both the gateway and the dashboard — one source of truth for the cross-service invariants",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.5.3",
    "vitest": "^4.1.8"
  }
}
