{
  "name": "@repo/env-kind",
  "private": true,
  "main": "./index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@repo/typescript-config": "*",
    "typescript": "^5.5.3",
    "vitest": "^4.1.8"
  }
}
