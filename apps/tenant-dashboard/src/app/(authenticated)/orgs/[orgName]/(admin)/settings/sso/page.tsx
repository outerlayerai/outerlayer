import { SSOSettings } from "@ee/features/sso/components/sso-settings";

export const metadata = {
  title: "Settings: SSO",
};

export default function SSOSettingsPage() {
  return <SSOSettings />;
}
