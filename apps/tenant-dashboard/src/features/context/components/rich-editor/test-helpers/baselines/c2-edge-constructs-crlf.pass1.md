# Edge constructs

<!-- this is an html comment, should not render -->

Paragraph with a hard line break using two trailing spaces.\
Second line of the same paragraph.

Paragraph with an explicit break tag.
Second line via br.

This has ~~strikethrough text~~ inline.

This sentence has a footnote reference.[^1]

[^1]: This is the footnote definition, with **bold** inside it.

A line with trailing spaces that should NOT be treated as a break (only one trailing space).
Next line.

Some `inline code` and a [reference link](https://example.com/reference "Reference Title") and an ![image](https://example.com/img.png "title text").
