// ----------------------------------------------------------------------

// Brand color values live in @repo/design-tokens — the shared source of truth
// the Claude Design preview bundle is also generated from. This module remains
// the app-side import surface for them.
export {
  BRAND_PRIMARY_LIGHT,
  BRAND_PRIMARY_DARK,
  BRAND_PRIMARY_MAIN,
  PLATFORM_ADMIN_PRIMARY_MAIN,
} from "@repo/design-tokens";
