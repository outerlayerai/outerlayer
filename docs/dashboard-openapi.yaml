# AUTO-GENERATED — DO NOT EDIT BY HAND.
# Source of truth: apps/tenant-dashboard/src/app/api/**/route.ts (withApi + Zod).
# Regenerate via `yarn workspace tenant-dashboard gen:openapi`. CI enforces no drift.
components:
  parameters: {}
  responses:
    InternalError:
      content:
        application/json:
          schema:
            properties:
              error:
                additionalProperties: true
                properties:
                  code:
                    type: string
                  details:
                    additionalProperties: true
                    type: object
                  message:
                    type: string
                required:
                  - code
                  - message
                type: object
            required:
              - error
            type: object
      description: Unexpected server error. The client has not malformed the request; the failure is on our side and has been logged. Safe to retry with backoff.
  schemas: {}
  securitySchemes:
    SessionCookie:
      description: Supabase session cookie set on sign-in. Dashboard-UI-only — machine clients should use the Gateway API (api.agentmark.co) with an API key instead.
      in: cookie
      name: sb-access-token
      type: apiKey
info:
  description: Internal HTTP API that the OuterLayer dashboard web app uses for tenant-scoped reads/writes. This surface is served by the same Next.js app as the UI and authenticates via Supabase session cookie. Not a public API — for programmatic access use the Gateway API at api.agentmark.co.
  title: OuterLayer Dashboard API
  version: "1.0"
openapi: 3.0.3
paths:
  /api/orgs/{orgName}/apps/{appId}/agents/blob/{sha256}:
    get:
      operationId: get-agents-blob
      parameters:
        - in: path
          name: orgName
          required: true
          schema:
            minLength: 1
            type: string
        - in: path
          name: appId
          required: true
          schema:
            minLength: 1
            type: string
        - in: path
          name: sha256
          required: true
          schema:
            pattern: ^[a-f0-9]{64}$
            type: string
        - in: query
          name: appId
          required: true
          schema:
            type: string
        - in: query
          name: token
          required: true
          schema:
            minLength: 1
            type: string
      responses:
        "200":
          description: Image bytes.
        "403":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Missing, altered, or expired image token.
        "404":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Not found.
        default:
          $ref: "#/components/responses/InternalError"
      summary: Agent image blob (content-addressed)
      tags:
        - Agents
  /api/orgs/{orgName}/apps/{appId}/member-roles:
    get:
      description: Lists per-app role assignments for the app, optionally filtered to one membership. Not gated by the `app_level_roles` entitlement — only by `app_member_role.read` — so an unlicensed org can still see assignments it created before downgrading.
      operationId: list-app-member-roles
      parameters:
        - description: Application ID (tenant-scoping). Every authenticated call passes this.
          in: query
          name: appId
          required: true
          schema:
            type: string
        - in: path
          name: orgName
          required: true
          schema:
            minLength: 1
            type: string
        - in: path
          name: appId
          required: true
          schema:
            minLength: 1
            type: string
        - in: query
          name: membershipId
          required: false
          schema:
            minLength: 1
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  properties:
                    appId:
                      type: string
                    appName:
                      type: string
                    createdAt:
                      type: string
                    id:
                      type: string
                    memberEmail:
                      type: string
                    memberName:
                      type: string
                    membershipId:
                      type: string
                    role:
                      enum:
                        - read
                        - write
                        - admin
                        - null
                      nullable: true
                      type: string
                    updatedAt:
                      nullable: true
                      type: string
                  required:
                    - id
                    - membershipId
                    - appId
                    - appName
                    - role
                    - memberName
                    - memberEmail
                    - createdAt
                    - updatedAt
                  type: object
                type: array
          description: App-member-role assignment list.
        "401":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Not authenticated. Management API keys are not supported on this endpoint — session only.
        "403":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Lacks `app_member_role.read`.
        default:
          $ref: "#/components/responses/InternalError"
      summary: List per-app role assignments
      tags:
        - App Member Roles
    post:
      description: 'Assigns a built-in per-app role (read/write/admin) to a membership. Requires the `app_level_roles` entitlement — on a self-hosted or unlicensed org without it, this fails closed with 403 and `error.reason: "entitlement_denied"` rather than creating the assignment.'
      operationId: assign-app-member-role
      parameters:
        - description: Application ID (tenant-scoping). Every authenticated call passes this.
          in: query
          name: appId
          required: true
          schema:
            type: string
        - in: path
          name: orgName
          required: true
          schema:
            minLength: 1
            type: string
        - in: path
          name: appId
          required: true
          schema:
            minLength: 1
            type: string
      requestBody:
        content:
          application/json:
            schema:
              properties:
                membershipId:
                  minLength: 1
                  type: string
                role:
                  enum:
                    - read
                    - write
                    - admin
                  type: string
              required:
                - membershipId
                - role
              type: object
      responses:
        "201":
          content:
            application/json:
              schema:
                properties:
                  app_id:
                    type: string
                  created_at:
                    type: string
                  created_by:
                    nullable: true
                    type: string
                  custom_role_id:
                    nullable: true
                    type: string
                  id:
                    type: string
                  membership_id:
                    type: string
                  role:
                    enum:
                      - read
                      - write
                      - admin
                    type: string
                  tenant_id:
                    type: string
                  updated_at:
                    nullable: true
                    type: string
                  updated_by:
                    nullable: true
                    type: string
                required:
                  - id
                  - membership_id
                  - app_id
                  - tenant_id
                  - role
                  - custom_role_id
                  - created_at
                  - created_by
                  - updated_at
                  - updated_by
                type: object
          description: The created app-member-role row.
        "400":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Invalid body, or the target membership is an org owner.
        "401":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Not authenticated. Management API keys are not supported on this endpoint — session only.
        "403":
          content:
            application/json:
              schema:
                properties:
                  error:
                    properties:
                      code:
                        enum:
                          - forbidden
                        type: string
                      entitlement:
                        properties:
                          currentLimit:
                            nullable: true
                            type: number
                          featureDisplayName:
                            type: string
                          featureKey:
                            type: string
                          isSelfServe:
                            type: boolean
                          pricing:
                            nullable: true
                            type: string
                          requiredTier:
                            type: string
                          requiredTierDisplayName:
                            type: string
                          requiredTierLimit:
                            nullable: true
                            type: number
                          upgradeUrl:
                            type: string
                        required:
                          - featureKey
                          - featureDisplayName
                          - requiredTier
                          - requiredTierDisplayName
                          - isSelfServe
                          - pricing
                          - upgradeUrl
                          - currentLimit
                          - requiredTierLimit
                        type: object
                      message:
                        type: string
                      reason:
                        enum:
                          - entitlement_denied
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: 'Lacks `app_member_role.insert` (plain `forbidden`), or the `app_level_roles` entitlement is absent (`forbidden` with `reason: "entitlement_denied"` and an `entitlement` upgrade payload).'
        default:
          $ref: "#/components/responses/InternalError"
      summary: Assign a per-app role
      tags:
        - App Member Roles
  /api/orgs/{orgName}/apps/{appId}/member-roles/{appMemberRoleId}:
    delete:
      description: 'Revokes a per-app role assignment; the membership falls back to its org-level role for the app. Requires the `app_level_roles` entitlement — on a self-hosted or unlicensed org without it, this fails closed with 403 and `error.reason: "entitlement_denied"` rather than revoking the assignment.'
      operationId: revoke-app-member-role
      parameters:
        - description: Application ID (tenant-scoping). Every authenticated call passes this.
          in: query
          name: appId
          required: true
          schema:
            type: string
        - in: path
          name: orgName
          required: true
          schema:
            minLength: 1
            type: string
        - in: path
          name: appId
          required: true
          schema:
            minLength: 1
            type: string
        - in: path
          name: appMemberRoleId
          required: true
          schema:
            minLength: 1
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                properties:
                  success:
                    type: boolean
                required:
                  - success
                type: object
          description: Acknowledgement of the revoke — not the deleted row.
        "401":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Not authenticated. Management API keys are not supported on this endpoint — session only.
        "403":
          content:
            application/json:
              schema:
                properties:
                  error:
                    properties:
                      code:
                        enum:
                          - forbidden
                        type: string
                      entitlement:
                        properties:
                          currentLimit:
                            nullable: true
                            type: number
                          featureDisplayName:
                            type: string
                          featureKey:
                            type: string
                          isSelfServe:
                            type: boolean
                          pricing:
                            nullable: true
                            type: string
                          requiredTier:
                            type: string
                          requiredTierDisplayName:
                            type: string
                          requiredTierLimit:
                            nullable: true
                            type: number
                          upgradeUrl:
                            type: string
                        required:
                          - featureKey
                          - featureDisplayName
                          - requiredTier
                          - requiredTierDisplayName
                          - isSelfServe
                          - pricing
                          - upgradeUrl
                          - currentLimit
                          - requiredTierLimit
                        type: object
                      message:
                        type: string
                      reason:
                        enum:
                          - entitlement_denied
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: 'Lacks `app_member_role.delete` (plain `forbidden`), or the `app_level_roles` entitlement is absent (`forbidden` with `reason: "entitlement_denied"` and an `entitlement` upgrade payload).'
        default:
          $ref: "#/components/responses/InternalError"
      summary: Revoke a per-app role assignment
      tags:
        - App Member Roles
    patch:
      description: 'Switches an existing per-app role assignment to a different built-in role (`role`) or a custom-role override (`customRoleId`) — exactly one of the two. Requires the `app_level_roles` entitlement — on a self-hosted or unlicensed org without it, this fails closed with 403 and `error.reason: "entitlement_denied"` rather than applying the change.'
      operationId: update-app-member-role
      parameters:
        - description: Application ID (tenant-scoping). Every authenticated call passes this.
          in: query
          name: appId
          required: true
          schema:
            type: string
        - in: path
          name: orgName
          required: true
          schema:
            minLength: 1
            type: string
        - in: path
          name: appId
          required: true
          schema:
            minLength: 1
            type: string
        - in: path
          name: appMemberRoleId
          required: true
          schema:
            minLength: 1
            type: string
      requestBody:
        content:
          application/json:
            schema:
              properties:
                customRoleId:
                  minLength: 1
                  type: string
                role:
                  enum:
                    - read
                    - write
                    - admin
                  type: string
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                properties:
                  app_id:
                    type: string
                  created_at:
                    type: string
                  created_by:
                    nullable: true
                    type: string
                  custom_role_id:
                    nullable: true
                    type: string
                  id:
                    type: string
                  membership_id:
                    type: string
                  role:
                    enum:
                      - read
                      - write
                      - admin
                    type: string
                  tenant_id:
                    type: string
                  updated_at:
                    nullable: true
                    type: string
                  updated_by:
                    nullable: true
                    type: string
                required:
                  - id
                  - membership_id
                  - app_id
                  - tenant_id
                  - role
                  - custom_role_id
                  - created_at
                  - created_by
                  - updated_at
                  - updated_by
                type: object
          description: The updated app-member-role row.
        "400":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Neither or both of `role`/`customRoleId` given.
        "401":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Not authenticated. Management API keys are not supported on this endpoint — session only.
        "403":
          content:
            application/json:
              schema:
                properties:
                  error:
                    properties:
                      code:
                        enum:
                          - forbidden
                        type: string
                      entitlement:
                        properties:
                          currentLimit:
                            nullable: true
                            type: number
                          featureDisplayName:
                            type: string
                          featureKey:
                            type: string
                          isSelfServe:
                            type: boolean
                          pricing:
                            nullable: true
                            type: string
                          requiredTier:
                            type: string
                          requiredTierDisplayName:
                            type: string
                          requiredTierLimit:
                            nullable: true
                            type: number
                          upgradeUrl:
                            type: string
                        required:
                          - featureKey
                          - featureDisplayName
                          - requiredTier
                          - requiredTierDisplayName
                          - isSelfServe
                          - pricing
                          - upgradeUrl
                          - currentLimit
                          - requiredTierLimit
                        type: object
                      message:
                        type: string
                      reason:
                        enum:
                          - entitlement_denied
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: 'Lacks `app_member_role.update` (plain `forbidden`), or the `app_level_roles` entitlement is absent (`forbidden` with `reason: "entitlement_denied"` and an `entitlement` upgrade payload).'
        default:
          $ref: "#/components/responses/InternalError"
      summary: Update a per-app role assignment
      tags:
        - App Member Roles
  /api/orgs/{orgName}/apps/{appId}/onboarding/checklist:
    get:
      description: Returns the onboarding setup state (checklist booleans + git provider) — drives the getting-started checklist, the setup card gate, and the soft banner.
      operationId: onboarding-checklist
      parameters:
        - in: path
          name: orgName
          required: true
          schema:
            minLength: 1
            type: string
        - in: path
          name: appId
          required: true
          schema:
            minLength: 1
            type: string
        - in: query
          name: appId
          required: true
          schema:
            minLength: 1
            type: string
      responses:
        "200":
          description: "`{ hasApiKey, hasTrace, hasTeammate, hasGitConnection, hasRepoLinked, provider }`."
        "400":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Invalid query params.
        "401":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Not authenticated.
        default:
          $ref: "#/components/responses/InternalError"
      summary: Getting-started checklist status
      tags:
        - Onboarding
  /api/orgs/{orgName}/custom-roles:
    get:
      description: Lists the org's custom roles with their permissions and member counts. Unlike the mutation endpoints below, listing isn't gated by the `custom_roles` entitlement — only by the `custom_role.read` permission — so an unlicensed org can still see roles it created before downgrading.
      operationId: list-custom-roles
      parameters:
        - description: Application ID (tenant-scoping). Every authenticated call passes this.
          in: query
          name: appId
          required: true
          schema:
            type: string
        - in: path
          name: orgName
          required: true
          schema:
            minLength: 1
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  properties:
                    created_at:
                      type: string
                    created_by:
                      nullable: true
                      type: string
                    description:
                      nullable: true
                      type: string
                    id:
                      type: string
                    memberCount:
                      type: number
                    name:
                      type: string
                    permissions:
                      items:
                        type: string
                      type: array
                    tenant_id:
                      type: string
                    updated_at:
                      nullable: true
                      type: string
                    updated_by:
                      nullable: true
                      type: string
                  required:
                    - id
                    - tenant_id
                    - name
                    - description
                    - created_at
                    - created_by
                    - updated_at
                    - updated_by
                    - permissions
                    - memberCount
                  type: object
                type: array
          description: Custom role list.
        "401":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Not authenticated. Management API keys are not supported on this endpoint — session only.
        "403":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Lacks `custom_role.read`.
        default:
          $ref: "#/components/responses/InternalError"
      summary: List custom roles
      tags:
        - Custom Roles
    post:
      description: 'Creates a custom role with the given permission set. Requires the `custom_roles` entitlement — on a self-hosted or unlicensed org without it, this fails closed with 403 and `error.reason: "entitlement_denied"` rather than creating the role.'
      operationId: create-custom-role
      parameters:
        - description: Application ID (tenant-scoping). Every authenticated call passes this.
          in: query
          name: appId
          required: true
          schema:
            type: string
        - in: path
          name: orgName
          required: true
          schema:
            minLength: 1
            type: string
      requestBody:
        content:
          application/json:
            schema:
              properties:
                description:
                  maxLength: 500
                  type: string
                name:
                  maxLength: 100
                  minLength: 1
                  type: string
                permissions:
                  items:
                    minLength: 1
                    type: string
                  minItems: 1
                  type: array
              required:
                - name
                - permissions
              type: object
      responses:
        "201":
          content:
            application/json:
              schema:
                properties:
                  created_at:
                    type: string
                  created_by:
                    nullable: true
                    type: string
                  description:
                    nullable: true
                    type: string
                  id:
                    type: string
                  memberCount:
                    type: number
                  name:
                    type: string
                  permissions:
                    items:
                      type: string
                    type: array
                  tenant_id:
                    type: string
                  updated_at:
                    nullable: true
                    type: string
                  updated_by:
                    nullable: true
                    type: string
                required:
                  - id
                  - tenant_id
                  - name
                  - description
                  - created_at
                  - created_by
                  - updated_at
                  - updated_by
                  - permissions
                  - memberCount
                type: object
          description: The created custom role.
        "400":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Invalid body, or a role with this name already exists.
        "401":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Not authenticated. Management API keys are not supported on this endpoint — session only.
        "403":
          content:
            application/json:
              schema:
                properties:
                  error:
                    properties:
                      code:
                        enum:
                          - forbidden
                        type: string
                      entitlement:
                        properties:
                          currentLimit:
                            nullable: true
                            type: number
                          featureDisplayName:
                            type: string
                          featureKey:
                            type: string
                          isSelfServe:
                            type: boolean
                          pricing:
                            nullable: true
                            type: string
                          requiredTier:
                            type: string
                          requiredTierDisplayName:
                            type: string
                          requiredTierLimit:
                            nullable: true
                            type: number
                          upgradeUrl:
                            type: string
                        required:
                          - featureKey
                          - featureDisplayName
                          - requiredTier
                          - requiredTierDisplayName
                          - isSelfServe
                          - pricing
                          - upgradeUrl
                          - currentLimit
                          - requiredTierLimit
                        type: object
                      message:
                        type: string
                      reason:
                        enum:
                          - entitlement_denied
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: 'Lacks `custom_role.insert` (plain `forbidden`), or the `custom_roles` entitlement is absent (`forbidden` with `reason: "entitlement_denied"` and an `entitlement` upgrade payload).'
        default:
          $ref: "#/components/responses/InternalError"
      summary: Create a custom role
      tags:
        - Custom Roles
  /api/orgs/{orgName}/custom-roles/{roleId}:
    delete:
      description: 'Deletes a custom role. If members are still assigned, `fallbackRole` (a built-in role, or `custom:<id>`) is required and they are reassigned to it. Requires the `custom_roles` entitlement — on a self-hosted or unlicensed org without it, this fails closed with 403 and `error.reason: "entitlement_denied"` rather than deleting the role.'
      operationId: delete-custom-role
      parameters:
        - description: Application ID (tenant-scoping). Every authenticated call passes this.
          in: query
          name: appId
          required: true
          schema:
            type: string
        - in: path
          name: orgName
          required: true
          schema:
            minLength: 1
            type: string
        - in: path
          name: roleId
          required: true
          schema:
            minLength: 1
            type: string
        - in: query
          name: fallbackRole
          required: false
          schema:
            minLength: 1
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                properties:
                  deleted:
                    enum:
                      - true
                    type: boolean
                  membersReassigned:
                    type: number
                required:
                  - deleted
                  - membersReassigned
                type: object
          description: The deleted role and how many members were reassigned.
        "400":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Members assigned and no `fallbackRole` given, or an invalid one.
        "401":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Not authenticated. Management API keys are not supported on this endpoint — session only.
        "403":
          content:
            application/json:
              schema:
                properties:
                  error:
                    properties:
                      code:
                        enum:
                          - forbidden
                        type: string
                      entitlement:
                        properties:
                          currentLimit:
                            nullable: true
                            type: number
                          featureDisplayName:
                            type: string
                          featureKey:
                            type: string
                          isSelfServe:
                            type: boolean
                          pricing:
                            nullable: true
                            type: string
                          requiredTier:
                            type: string
                          requiredTierDisplayName:
                            type: string
                          requiredTierLimit:
                            nullable: true
                            type: number
                          upgradeUrl:
                            type: string
                        required:
                          - featureKey
                          - featureDisplayName
                          - requiredTier
                          - requiredTierDisplayName
                          - isSelfServe
                          - pricing
                          - upgradeUrl
                          - currentLimit
                          - requiredTierLimit
                        type: object
                      message:
                        type: string
                      reason:
                        enum:
                          - entitlement_denied
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: 'Lacks `custom_role.delete` (plain `forbidden`), or the `custom_roles` entitlement is absent (`forbidden` with `reason: "entitlement_denied"` and an `entitlement` upgrade payload).'
        default:
          $ref: "#/components/responses/InternalError"
      summary: Delete a custom role
      tags:
        - Custom Roles
    get:
      description: Returns a custom role with its permissions and assigned members. Not gated by the `custom_roles` entitlement — only by `custom_role.read` — matching the list endpoint.
      operationId: get-custom-role
      parameters:
        - description: Application ID (tenant-scoping). Every authenticated call passes this.
          in: query
          name: appId
          required: true
          schema:
            type: string
        - in: path
          name: orgName
          required: true
          schema:
            minLength: 1
            type: string
        - in: path
          name: roleId
          required: true
          schema:
            minLength: 1
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                properties:
                  created_at:
                    type: string
                  created_by:
                    nullable: true
                    type: string
                  description:
                    nullable: true
                    type: string
                  id:
                    type: string
                  members:
                    items:
                      properties:
                        membershipId:
                          type: string
                        userEmail:
                          type: string
                        userName:
                          type: string
                      required:
                        - membershipId
                        - userName
                        - userEmail
                      type: object
                    type: array
                  name:
                    type: string
                  permissions:
                    items:
                      type: string
                    type: array
                  tenant_id:
                    type: string
                  updated_at:
                    nullable: true
                    type: string
                  updated_by:
                    nullable: true
                    type: string
                required:
                  - id
                  - tenant_id
                  - name
                  - description
                  - created_at
                  - created_by
                  - updated_at
                  - updated_by
                  - permissions
                  - members
                type: object
          description: The custom role.
        "401":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Not authenticated. Management API keys are not supported on this endpoint — session only.
        "403":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Lacks `custom_role.read`.
        default:
          $ref: "#/components/responses/InternalError"
      summary: Get a custom role
      tags:
        - Custom Roles
    patch:
      description: "Updates a custom role's name, description, and/or permission set. Requires the `custom_roles` entitlement — on a self-hosted or unlicensed org without it, this fails closed with 403 and `error.reason: \"entitlement_denied\"` rather than applying the update."
      operationId: update-custom-role
      parameters:
        - description: Application ID (tenant-scoping). Every authenticated call passes this.
          in: query
          name: appId
          required: true
          schema:
            type: string
        - in: path
          name: orgName
          required: true
          schema:
            minLength: 1
            type: string
        - in: path
          name: roleId
          required: true
          schema:
            minLength: 1
            type: string
      requestBody:
        content:
          application/json:
            schema:
              properties:
                description:
                  maxLength: 500
                  type: string
                name:
                  maxLength: 100
                  minLength: 1
                  type: string
                permissions:
                  items:
                    minLength: 1
                    type: string
                  minItems: 1
                  type: array
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                properties:
                  created_at:
                    type: string
                  created_by:
                    nullable: true
                    type: string
                  description:
                    nullable: true
                    type: string
                  id:
                    type: string
                  memberCount:
                    type: number
                  name:
                    type: string
                  permissions:
                    items:
                      type: string
                    type: array
                  tenant_id:
                    type: string
                  updated_at:
                    nullable: true
                    type: string
                  updated_by:
                    nullable: true
                    type: string
                required:
                  - id
                  - tenant_id
                  - name
                  - description
                  - created_at
                  - created_by
                  - updated_at
                  - updated_by
                  - permissions
                  - memberCount
                type: object
          description: The updated custom role.
        "400":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Invalid body, or a role with this name already exists.
        "401":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Not authenticated. Management API keys are not supported on this endpoint — session only.
        "403":
          content:
            application/json:
              schema:
                properties:
                  error:
                    properties:
                      code:
                        enum:
                          - forbidden
                        type: string
                      entitlement:
                        properties:
                          currentLimit:
                            nullable: true
                            type: number
                          featureDisplayName:
                            type: string
                          featureKey:
                            type: string
                          isSelfServe:
                            type: boolean
                          pricing:
                            nullable: true
                            type: string
                          requiredTier:
                            type: string
                          requiredTierDisplayName:
                            type: string
                          requiredTierLimit:
                            nullable: true
                            type: number
                          upgradeUrl:
                            type: string
                        required:
                          - featureKey
                          - featureDisplayName
                          - requiredTier
                          - requiredTierDisplayName
                          - isSelfServe
                          - pricing
                          - upgradeUrl
                          - currentLimit
                          - requiredTierLimit
                        type: object
                      message:
                        type: string
                      reason:
                        enum:
                          - entitlement_denied
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: 'Lacks `custom_role.update` (plain `forbidden`), or the `custom_roles` entitlement is absent (`forbidden` with `reason: "entitlement_denied"` and an `entitlement` upgrade payload).'
        default:
          $ref: "#/components/responses/InternalError"
      summary: Update a custom role
      tags:
        - Custom Roles
  /api/orgs/{orgName}/has-traces:
    get:
      description: "Returns `{ hasTraces: boolean }` — used to drive the onboarding placeholder in the dashboard."
      operationId: has-traces
      parameters:
        - in: path
          name: orgName
          required: true
          schema:
            minLength: 1
            type: string
        - in: query
          name: appId
          required: true
          schema:
            minLength: 1
            type: string
      responses:
        "200":
          description: "`{ hasTraces: boolean }`."
        "400":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Invalid query params.
        "401":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Not authenticated.
        default:
          $ref: "#/components/responses/InternalError"
      summary: Onboarding existence check
      tags:
        - Analytics
  /api/orgs/{orgName}/members:
    get:
      description: Returns active memberships and pending invites for the resolved request tenant. Requires `membership.read`.
      operationId: list-members
      parameters:
        - description: Application ID (tenant-scoping). Every authenticated call passes this.
          in: query
          name: appId
          required: true
          schema:
            type: string
        - in: path
          name: orgName
          required: true
          schema:
            minLength: 1
            type: string
      responses:
        "200":
          description: "`{ members: TenantMember[] }`."
        "401":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Not authenticated.
        "403":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Missing `membership.read`.
        default:
          $ref: "#/components/responses/InternalError"
      summary: List org members and pending invites
      tags:
        - Members
  /api/orgs/{orgName}/members/invites:
    post:
      description: Sends an org invite. Requires `membership.insert`.
      operationId: invite-member
      parameters:
        - description: Application ID (tenant-scoping). Every authenticated call passes this.
          in: query
          name: appId
          required: true
          schema:
            type: string
        - in: path
          name: orgName
          required: true
          schema:
            minLength: 1
            type: string
      requestBody:
        content:
          application/json:
            schema:
              properties:
                appRoles:
                  items:
                    properties:
                      appId:
                        minLength: 1
                        type: string
                      role:
                        enum:
                          - read
                          - write
                          - admin
                        type: string
                    required:
                      - appId
                      - role
                    type: object
                  type: array
                customRoleId:
                  minLength: 1
                  type: string
                email:
                  format: email
                  minLength: 1
                  type: string
                name:
                  maxLength: 100
                  minLength: 1
                  type: string
                role:
                  enum:
                    - owner
                    - admin
                    - write
                    - read
                    - disabled
                  type: string
              required:
                - name
                - email
                - role
              type: object
      responses:
        "200":
          description: "`{ membershipId }`."
        "400":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Invalid input, or a business-rule denial (e.g. only owners can invite owners).
        "401":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Not authenticated.
        "403":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Missing `membership.insert`, or the org's plan denies this invite (`entitlement_denied`, with `{ entitlement }` extras).
        default:
          $ref: "#/components/responses/InternalError"
      summary: Invite a member
      tags:
        - Members
  /api/orgs/{orgName}/members/invites/{inviteId}/resend:
    post:
      description: Resends the invite email for a pending membership. Requires `membership.insert`.
      operationId: resend-member-invite
      parameters:
        - description: Application ID (tenant-scoping). Every authenticated call passes this.
          in: query
          name: appId
          required: true
          schema:
            type: string
        - in: path
          name: orgName
          required: true
          schema:
            minLength: 1
            type: string
        - in: path
          name: inviteId
          required: true
          schema:
            minLength: 1
            type: string
      responses:
        "200":
          description: "`{ success: true }`."
        "401":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Not authenticated.
        "403":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Missing `membership.insert`.
        "404":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: No pending invite with that id in this org.
        default:
          $ref: "#/components/responses/InternalError"
      summary: Resend a pending invite
      tags:
        - Members
  /api/orgs/{orgName}/members/{userId}:
    delete:
      description: Removes a member from the org. Requires `membership.delete`.
      operationId: remove-member
      parameters:
        - description: Application ID (tenant-scoping). Every authenticated call passes this.
          in: query
          name: appId
          required: true
          schema:
            type: string
        - in: path
          name: orgName
          required: true
          schema:
            minLength: 1
            type: string
        - in: path
          name: userId
          required: true
          schema:
            minLength: 1
            type: string
      responses:
        "200":
          description: "`{ success: true }`."
        "400":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Business-rule denial (e.g. last-owner protection).
        "401":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Not authenticated.
        "403":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Missing `membership.delete`.
        "404":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: No member with that id in this org.
        default:
          $ref: "#/components/responses/InternalError"
      summary: Remove a member from the org
      tags:
        - Members
    patch:
      description: Changes a member's built-in or custom role. Requires `membership.update`.
      operationId: change-member-role
      parameters:
        - description: Application ID (tenant-scoping). Every authenticated call passes this.
          in: query
          name: appId
          required: true
          schema:
            type: string
        - in: path
          name: orgName
          required: true
          schema:
            minLength: 1
            type: string
        - in: path
          name: userId
          required: true
          schema:
            minLength: 1
            type: string
      requestBody:
        content:
          application/json:
            schema:
              properties:
                customRoleId:
                  minLength: 1
                  nullable: true
                  type: string
                role:
                  enum:
                    - owner
                    - admin
                    - write
                    - read
                    - disabled
                  type: string
              required:
                - role
              type: object
      responses:
        "200":
          description: "`{ success: true }`."
        "400":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Business-rule denial (e.g. last-owner protection).
        "401":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Not authenticated.
        "403":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Missing `membership.update`.
        "404":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: No member with that id in this org.
        default:
          $ref: "#/components/responses/InternalError"
      summary: Change a member's role
      tags:
        - Members
  /api/orgs/{orgName}/roles:
    get:
      description: Static catalog of the built-in org roles (owner/admin/write/read/disabled).
      operationId: list-roles
      parameters:
        - description: Application ID (tenant-scoping). Every authenticated call passes this.
          in: query
          name: appId
          required: true
          schema:
            type: string
        - in: path
          name: orgName
          required: true
          schema:
            minLength: 1
            type: string
      responses:
        "200":
          description: "`{ roles: BuiltInRole[] }`."
        "401":
          content:
            application/json:
              schema:
                properties:
                  error:
                    additionalProperties:
                      nullable: true
                    properties:
                      code:
                        type: string
                      message:
                        type: string
                    required:
                      - code
                      - message
                    type: object
                required:
                  - error
                type: object
          description: Not authenticated.
        default:
          $ref: "#/components/responses/InternalError"
      summary: List built-in org roles
      tags:
        - Members
security:
  - SessionCookie: []
servers:
  - description: Production dashboard
    url: https://app.outerlayer.ai
  - description: Local dev server
    url: http://localhost:3002
